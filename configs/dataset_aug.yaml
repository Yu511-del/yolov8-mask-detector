# YOLOv8 augmented dataset configuration
# Paths are relative to the project root
path: ./data/augmented
train: images/train
val: images/val
test: images/test

# Number of classes
nc: 3

# Class names
names:
  0: with_mask
  1: without_mask
  2: mask_weared_incorrect
